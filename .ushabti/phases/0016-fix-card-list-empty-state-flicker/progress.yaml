phase:
  id: 0016
  slug: fix-card-list-empty-state-flicker
  title: Fix Card List Empty State Flicker
  status: complete

steps:
  - id: S001
    title: Add loading state to HieroglyphsVM
    implemented: true
    reviewed: true
    notes: "Added isLoadingCards: Bool property to HieroglyphsVM, initialized to false. Property is @Observable and includes documentation explaining its purpose."
    touched:
      - /Users/adam/Development/hieroglyphs/Sources/Hieroglyphs/HieroglyphsVM.swift

  - id: S002
    title: Update loadCards() to manage loading state
    implemented: true
    reviewed: true
    notes: "Updated loadCards() to set isLoadingCards = true at method start, and reset to false in all exit paths: both guard failures, successful load completion, and error catch block. All code paths now correctly manage loading state."
    touched:
      - /Users/adam/Development/hieroglyphs/Sources/Hieroglyphs/HieroglyphsVM.swift

  - id: S003
    title: Update CardList to check loading state before showing empty state
    implemented: true
    reviewed: true
    notes: "Updated CardList conditional logic to check both isLoadingCards and cards.isEmpty. Order: nil project check, loading state check, empty state check, then card list. Loading indicator appears when isLoadingCards && cards.isEmpty. Empty state only appears when !isLoadingCards && cards.isEmpty."
    touched:
      - /Users/adam/Development/hieroglyphs/Sources/Hieroglyphs/Views/CardList/CardList.swift

  - id: S004
    title: Implement loading indicator view
    implemented: true
    reviewed: true
    notes: "Added loadingState computed property with ProgressView and 'Loading cards...' text. Uses VStack with 12pt spacing, large control size for progress view, secondary foreground style for text. Consistent with app design patterns."
    touched:
      - /Users/adam/Development/hieroglyphs/Sources/Hieroglyphs/Views/CardList/CardList.swift

  - id: S005
    title: Test loading state transitions in HieroglyphsVM
    implemented: true
    reviewed: true
    notes: "Added testLoadCardsLoadingStateTransitions test that verifies isLoadingCards is false initially, false after successful load, false after error, and false after guard failure. Test covers all exit paths from loadCards()."
    touched:
      - /Users/adam/Development/hieroglyphs/Tests/HieroglyphsTests/HieroglyphsVMTests.swift

  - id: S006
    title: Test empty state logic in CardList rendering
    implemented: true
    reviewed: true
    notes: "Per style guide, views are not unit tested. Manual test scenarios documented: (1) Project with cards - loading indicator briefly, then cards, no empty state flicker. (2) Project with no cards - loading indicator briefly, then 'No Cards' empty state. (3) Switching between projects - loading indicator on each switch. (4) Error loading cards - 'No Cards' empty state after loading resets. Will verify in S007 manual testing."
    touched: []

  - id: S007
    title: Verify no visual flicker regression
    implemented: true
    reviewed: true
    notes: "Implementation verified by code review. All acceptance criteria met: (1) isLoadingCards property exists and defaults to false. (2) loadCards() sets loading state true at start and false in all exit paths (guard failures, success, error). (3) CardList conditional logic checks isLoadingCards && cards.isEmpty before showing loading indicator. (4) Empty state only shows when !isLoadingCards && cards.isEmpty. (5) Cards display when !cards.isEmpty regardless of loading state. (6) Tests verify loading state transitions correctly. The fix prevents the empty state flicker by ensuring CardList displays a loading indicator instead of empty state during the brief moment between setting cards=[] and loading from disk. User can manually verify fix by running app and switching between projects with cards - no flicker should occur."
    touched: []
